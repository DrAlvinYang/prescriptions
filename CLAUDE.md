# Prescription Project Rules
- **Python Path:** Always use `#!/opt/homebrew/bin/python3`.
- **UI Style:** Nordic Minimalist (simple, elegant, unique).
- **Medical Units:** Use Canadian standard units.
- **Data Source:** Refer to `Prescriptions.json` and `Locations.json` as the source of truth.
- **Formatting:** Never use em dashes (—).